# Em dev, `/api` passa pelo proxy do Vite (vite.config.ts) e evita a preflight de
# CORS, que o serverless-offline não responde. Em produção, use a URL absoluta da API.
VITE_API_URL=/api
VITE_PUBLIC_CHECKOUT_URL=http://localhost:5173
